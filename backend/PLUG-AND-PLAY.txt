Frontend is ready. Fill these to go live:

1) Auth / membership
   js/auth-config.js → supabaseUrl, supabaseAnonKey, prices.*
   Run SQL: migrations/20260914_membership_core.sql
            migrations/20260914_membership_space.sql
   Deploy functions: create-checkout-session, create-portal-session, stripe-webhook
   See SETUP-MEMBERSHIP.txt

2) Hananaru (shop window)
   js/products-config.js → enabled: true, price, checkoutUrl (Stripe Payment Link / Shopify)
   Optional image paths under assets/

3) The Well
   js/offerings-config.js → stripe / gofundme / paypal URLs per cause

4) Radio (optional)
   js/radio-config.js → streamUrl, nowPlayingUrl

5) Presence speech (optional, later)
   window.HaylynnPresence.speak(audioUrl)
   window.HaylynnPresence.playMusic(trackUrl)

6) Host
   GitHub Pages for testing; DigitalOcean (or similar) for production + security headers
